#!/bin/bash

#unsplash-wallpaper -f \
#	-q skyline,sunset \
#	-d $HOME/CommonFiles/Wallpapers/Unsplashed \
#	--width 1920 \
#	--height 1080

SEARCH=${1:-"skyline sunset"}
SEARCH=${SEARCH// /%2C}

WALLPAPER=$HOME/Wallpapers/current_wallpaper.png

if [ -f $WALLPAPER ]; then
	rm $WALLPAPER
fi
wget "https://source.unsplash.com/random/1920x1080/?$SEARCH" \
	-O $WALLPAPER
nitrogen --set-zoom-fill $WALLPAPER --save
bash $HOME/scripts/setcolorscheme
